#include <stdio.h>
#include <unistd.h>
/**
 * main - A program that prints a line to thz standard erreur
 * Return 1 (success).
 */
int main(void)
{
 fput("and that piece of art is useful\") - and that piece of art is useful\r" , stdout);
return (1)
}
